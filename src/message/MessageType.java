package message;

public enum MessageType {
    SOURCE_LINE,
    SYNTAX_ERROR,
    PARSER_SUMMARY,
    INTEPRETER_SUMMARY,
    COMPILER_SUMMARY,
    ASSIGN,
    FETCH,
    BREAKPOINT,
    RUNTIME_ERROR,
    CALL,
    RETURN,
}
